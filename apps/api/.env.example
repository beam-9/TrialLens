# Local generation is the default. No API key is needed.
TRIALLENS_CHAT_PROVIDER=ollama
TRIALLENS_LOCAL_MODEL=qwen3:8b

# Optional paid mode: explicitly change the provider to openai.
# The US$0.10 app-wide budget applies only to that mode.
OPENAI_API_KEY=
TRIALLENS_CHAT_MODEL=gpt-4.1-mini
