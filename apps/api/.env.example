# Set both in the backend process environment; never expose keys as NEXT_PUBLIC_*.
OPENAI_API_KEY=
# A Responses API model supporting strict structured outputs, available to your account.
TRIALLENS_CHAT_MODEL=gpt-4.1-mini
